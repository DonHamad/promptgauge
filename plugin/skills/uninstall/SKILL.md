---
name: uninstall
description: Remove PromptGauge status-line setup while keeping local history. Run this before uninstalling the plugin.
disable-model-invocation: true
---

Run this command and show its output to the user unchanged. Do not add explanation.

```bash
promptgauge uninstall
```

Then tell the user they can uninstall the plugin with `/plugin uninstall promptgauge@promptgauge`.
