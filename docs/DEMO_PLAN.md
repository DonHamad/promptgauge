# Demo plan

Future 20–40 second recording. Do not publish a fake live quota demo.

## Sequence

1. Show Claude Code (interactive session, no private prompt text on camera).
2. Show PromptGauge integration: `promptgauge statusline status` and `promptgauge hooks status`.
3. Show prompt activity: `promptgauge prompts` (IDs only, never prompt bodies).
4. Show the local report: `promptgauge status` / `promptgauge doctor`.
5. Show the privacy claim: local JSONL, no PromptGauge cloud, no OAuth access.
6. Show the GitHub repository: https://github.com/DonHamad/promptgauge

## Subscription quota segment

Record the 5-hour / 7-day quota portion **only after validated on an actual Pro/Max account**.

Until then, the demo may show:

- installer success
- lifecycle rows with `N/A` deltas
- doctor lines that say `UNAVAILABLE`

It must not show invented percentages.
